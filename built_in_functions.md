abs() — возвращает абсолютное значение числа

aiter() — возвращает асинхронный итератор для асинхронного итерируемого объекта

all() — возвращает True, если все элементы итерируемого объекта истинны

any() — возвращает True, если хотя бы один элемент итерируемого объекта истинен

ascii() — возвращает строку с представлением объекта, заменяя символы, не входящие в ASCII, на escape-последовательности

bin() — преобразует целое число в двоичную строку

bool() — возвращает логическое значение объекта

breakpoint() — входит в отладчик в точке вызова

bytearray() — возвращает новый массив байтов

bytes() — возвращает новый объект bytes

callable() — возвращает True, если объект вызываем (функция, метод, класс)

chr() — возвращает символ по коду Unicode

classmethod() — преобразует метод в метод класса

compile() — компилирует исходный код в объект кода или AST

complex() — возвращает комплексное число (real + imag*1j)

delattr() — удаляет именованный атрибут объекта

dict() — создает новый словарь

dir() — возвращает список имен в текущей области видимости

divmod() — возвращает кортеж (частное, остаток) от деления

enumerate() — возвращает объект enumerate с индексами и элементами

eval() — вычисляет и выполняет выражение

exec() — выполняет Python код динамически

filter() — создает итератор из элементов, для которых функция возвращает True

float() — возвращает число с плавающей точкой

format() — форматирует значение в строку

frozenset() — создает неизменяемое множество

getattr() — возвращает значение именованного атрибута объекта

globals() — возвращает словарь глобальных переменных текущего модуля

hasattr() — возвращает True, если объект имеет именованный атрибут

hash() — возвращает хеш-значение объекта

help() — вызывает встроенную справку

hex() — преобразует целое число в шестнадцатеричную строку

id() — возвращает идентификатор (адрес в памяти) объекта

input() — читает строку из стандартного ввода

int() — возвращает целое число

isinstance() — возвращает True, если объект является экземпляром класса

issubclass() — возвращает True, если класс является подклассом другого класса

iter() — возвращает объект итератора

len() — возвращает длину объекта

list() — создает список или преобразует в список

locals() — возвращает словарь локальных переменных

map() — применяет функцию к каждому элементу итерируемого объекта

max() — возвращает наибольший элемент

memoryview() — возвращает объект представления памяти

min() — возвращает наименьший элемент

next() — возвращает следующий элемент итератора

object() — создает новый базовый объект

oct() — преобразует целое число в восьмеричную строку

open() — открывает файл и возвращает файловый объект

ord() — возвращает код Unicode символа

pow() — возвращает base в степени exp

print() — выводит объекты в стандартный поток вывода

property() — создает управляемый атрибут класса

range() — создает последовательность чисел

repr() — возвращает строку с представлением объекта

reversed() — возвращает обратный итератор

round() — округляет число до указанной точности

set() — создает новое множество

setattr() — устанавливает значение атрибута объекта

slice() — создает объект срезов

sorted() — возвращает отсортированный список

staticmethod() — преобразует метод в статический метод

str() — возвращает строковое представление объекта

sum() — суммирует элементы итерируемого объекта

super() — возвращает прокси-объект для доступа к методам родительского класса

tuple() — создает кортеж или преобразует в кортеж

type() — возвращает тип объекта

vars() — возвращает атрибут __dict__ объекта

zip() — объединяет несколько итерируемых объектов в параллельную итерацию